size(width, height)
point(x, y)
square(x, y, size)

rect(x, y, width, height)
triangle(x1, y1, x2, y2, x3, y3)
ellipse(x, y, width, height)

fill(red, green, blue)
line(x1, y1, x2, y2)
arc(x1, x2, width, height, angle rad (start), angle rad (end))

text(size, x, y)

// example of iteration: for(int i=0; i<5; i=i+1) {}

// example of triangle: triangle(x, y - size / 2, x - size / 2, y + size / 2, x + size / 2, y + size / 2);



